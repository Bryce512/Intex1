<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login</title>
    <link rel="stylesheet" href="/styles/styles.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css" rel="stylesheet">
    <link rel="icon" type="image/x-icon" href="https://images.squarespace-cdn.com/content/v1/5b5a266fcef372310148d6d6/1532843250594-O9ODAV19O8HYI57VCZ1G/favicon.ico?format=100w"/>
  <style>
    :root {
    --primary-color: #34b3a8;
    --primary-light: #B0E8E3;
    --secondary-color: #EF767A;
    --secondary-light: #F9C8CA;
    --black: #000;
    --white: #fff;
    --dark-grey: #333333;
    --font-size-large: 2rem;
    --font-size-small: 1rem;
    }

    .login-wrapper {
        font-family: Arial, sans-serif;
        background-color: #f4f7fc;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        color: #333;
        top:63px;
        left:0px;
        }

        /* Container for login form */
        .login-container {
        background-color: #fff;
        padding: 30px 40px;
        border-radius: 8px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        width: 100%;
        max-width: 400px;
        text-align: center;
        }

        /* Title styling */
        .login-container h1 {
        font-size: 2rem;
        margin-bottom: 20px;
        color: #333;
        }

        /* Form styling */
        form {
        width: 100%;
        }

        .form-group {
        margin-bottom: 20px;
        text-align: left;
        }

        label {
        display: block;
        font-size: 0.9rem;
        margin-bottom: 5px;
        color: #555;
        }

        input[type="text"], input[type="password"] {
        width: 100%;
        padding: 10px;
        font-size: 1rem;
        border: 1px solid #ddd;
        border-radius: 4px;
        margin-top: 5px;
        color: #333;
        }

        input[type="text"]:focus, input[type="password"]:focus {
        outline: none;
        border-color: #0056b3;
        }

        /* Button styling */
        .login-btn {
        width: 100%;
        padding: 12px;
        font-size: 1rem;
        background-color: var(--primary-color);
        color: white;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        transition: background-color 0.3s;
        }

        .login-btn:hover {
        background-color: var(--primary-light);
        }

        /* Error message styling */
        .error-message {
        margin-top: 15px;
        padding: 10px;
        background-color: #f8d7da;
        color: #721c24;
        border-radius: 4px;
        font-size: 0.9rem;
        border: 1px solid #f5c6cb;
        }

        .home{
            width: auto;
            position: absolute;
            top: 4%;
            left: 2%;
        }
    </style>
    <link rel="stylesheet" href="styles/styles.css">
</head>
<body>
    <a class="login-btn home" href="/">Back to Home</a>
    <div class="login-wrapper">
            <div class="login-container">
            <h1>Admin Login</h1>
            <form action="/login" method="POST">
                <div class="form-group">
                    <label for="username">Username:</label>
                    <input 
                        type="text" 
                        id="username" 
                        name="username" 
                        required 
                        placeholder="Enter username"
                    >
                </div>
                
                <div class="form-group">
                    <label for="password">Password:</label>
                    <input 
                        type="password" 
                        id="password" 
                        name="password" 
                        required 
                        placeholder="Enter password"
                    >
                </div>

                <button type="submit" class="login-btn">Login</button>
            </form>
            
            <% if (locals.error) { %>
                <div class="error-message">
                    <%= error %>
                </div>
            <% } %>
        </div>
    </div>
    
</body>
</html>